---
import heroImage1 from "assets/mmm_optimized_.jpg";

import heroImage4 from "assets/soll_optimized_.jpg"; // Asegúrate de tener más imágenes

import footer from "assets/ss2_optimized_.jpg";

import redes from "assets/ss4.jpg";

import sombrero from "assets/imgslider_optimized_.jpg";

import conti from "assets/conti_optimized_.jpg";
---

<br />
<div class="flex flex-wrap">
  <div class="md:w-1/2">
    <!-- Carrusel -->
    <div class="carousel-container">
      <div class="carousel-slide">
        <img src={footer.src} alt="Redes Solaria" />
      </div>
      <div class="carousel-slide">
        <img src={heroImage1.src} alt="Campos Paneles" />
      </div>
      <div class="carousel-slide">
        <img src={redes.src} alt="Panel Solar en Solaria" />
      </div>
      <div class="carousel-slide">
        <img src={heroImage4.src} alt="Sistemas de paneles en solaria" />
      </div>
      <div class="carousel-slide">
        <img src={sombrero.src} alt="Los mejores paneles" />
      </div>
      <div class="carousel-slide">
        <img src={conti.src} alt="Panel" />
      </div>
    </div>
  </div>
  <br />
  <div class="md:w-1/2 text-center">
    <!-- Texto a la derecha -->
    <p class="text-xl text-orange-400 lg:text-3xl tracking-tight">
      ¿Cuál es el costo de un panel solar en México?
    </p>
    <p class="text-lg text-center" style="padding:15px">
      El costo de un panel solar para un sistema residencial en México varía
      típicamente entre $8,000 y $10,000 pesos por panel, incluyendo la
      instalación, con la capacidad de generar ahorros de entre el 80% y el
      100% en el consumo eléctrico del hogar.<br /><br />
      Debido a la alta radiación solar en México, la instalación de paneles solares
      representa una de las inversiones más rentables para los hogares en diversas
      ciudades del país. Para proporcionarte una cotización precisa, necesitamos
      dos detalles clave:<br /><br />
      1. El monto aproximado de tu factura mensual de electricidad.<br />
      2. La ubicación específica donde planeas instalar los paneles. <br />
      <p class="text-lg text-center" style="padding:15px">
        Con esta información, podemos ofrecerte una cotización personalizada que
        te ayude a evaluar los beneficios y la viabilidad económica de adoptar
        la energía solar en tu residencia.
      </p>
    </p>
  </div>
</div>

<div></div>

<style>
  .carousel-container {
    width: 90%; /* Cambia el porcentaje según lo que desees */
    margin: 0 auto; /* Centra el carrusel en la página */
    height: auto;
    overflow: hidden;
    border-radius: 5px;
  }

  .carousel-slide {
    position: relative;
    background-attachment: fixed;
    background-size: cover;
    background-position: center;
    height: 65vh;
    margin-left: 0;
    margin-right: 0;
    display: none;
    transition: 2s ease;
  }

  .carousel-slide.active {
    display: block;
  }

  .carousel-slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 2s ease;
  }

  .carousel-button-prev,
  .carousel-button-next {
    position: absolute;
    top: 48%;
    transform: translateY(-50%);
    background-color: rgba(0, 0, 0, 0.4);
    color: white;
    border: none;
    padding: 10px;
    cursor: pointer;
    z-index: 10;
    border-radius: 50%;
    box-shadow: 2px 2px 8px rgba(0, 0, 0, 0.4);
    transition:
      background-color 0.3s,
      transform 0.3s;
  }

  .carousel-button-prev:hover,
  .carousel-button-next:hover {
    background-color: #2ca59c; /* Color de fondo al hacer hover */
    transform: translateY(-50%) scale(1.1); /* Escalado al hacer hover */
  }
  .carousel-button-prev {
    left: 10px; /* Ajusta la distancia del botón izquierdo al borde */
  }

  .carousel-button-next {
    right: 10px; /* Ajusta la distancia del botón derecho al borde */
  }

  @media (max-width: 520px) {
    .carousel-slide h1 {
      text-align: center; /* Centra el texto horizontalmente */
      transform: translateY(-50%); /* Ajusta la posición vertical del texto */
      margin-right: 200px;
      padding: 5px;
      position: absolute;
      display: block;
    }
  }
</style>

<script>
  let currentSlide = 0;
  const slides = document.querySelectorAll(".carousel-slide");
  const totalSlides = slides.length;

  function showNextSlide() {
    if (slides[currentSlide]) {
      slides[currentSlide].classList.remove("active");
      currentSlide = (currentSlide + 1) % totalSlides;
      if (slides[currentSlide]) {
        slides[currentSlide].classList.add("active");
      }
    }
  }

  document.querySelectorAll(".carousel-button-next").forEach((button) => {
    button.addEventListener("click", showNextSlide);
  });

  document.querySelectorAll(".carousel-button-prev").forEach((button) => {
    button.addEventListener("click", () => {
      if (slides[currentSlide]) {
        slides[currentSlide].classList.remove("active");
        currentSlide = (currentSlide - 1 + totalSlides) % totalSlides;
        if (slides[currentSlide]) {
          slides[currentSlide].classList.add("active");
        }
      }
    });
  });

  // Initialize the first slide as active
  slides[currentSlide].classList.add("active");

  // Set interval to change slide every 3 seconds
  setInterval(showNextSlide, 3200);
</script>
